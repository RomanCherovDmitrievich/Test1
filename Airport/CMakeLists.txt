add_subdirectory(Src)
if(NOT AIRPORT_TESTING)
  message(STATUS "Тестирование проекта Airport выключено.")
elseif(IS_SUBPROJECT)
  message(STATUS "Airport не тестируется в режиме подмодуля.")
else()
  add_subdirectory(Tests)
endif()
add_subdirectory(Doc)
